console.log('utlis.js');

export const square = (x) => x * x;
export const add = (a, b) => a + b;
const substract = (a, b) => a - b;

// export {square, add, substract as default};

export default substract;

// export { add };

// default export, name exports